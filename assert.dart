void main(){
 //assert is  method to check for the error It takes conditions as an argument. 
 //If the condition is true, nothing happens. If a condition is false, it will raise an error.
 //dart --enable-asserts file_name.dart
   var age = 22;
   assert(age!=22, "Age must be 22"); //if the condition is true  nothing happen
}